<?xml version="1.0"?>
<configuration>
  <system.web>
    <!-- Match the prof’s expected framework -->
    <compilation debug="true" targetFramework="4.0" />
  </system.web>

  <system.webServer>
    <!-- Let IIS show the folder contents (avoids 403 when browsing service folder) -->
    <directoryBrowse enabled="true" />
  </system.webServer>
</configuration>
